STEP 1: ALL NODES - System Prep + Docker (run first)
apt update && apt upgrade -y
swapoff -a && sed -i '/ swap / s/^/#/' /etc/fstab
apt install -y docker.io
systemctl enable --now docker
docker run hello-world  # TEST: Must work

STEP 2: ALL NODES - Install cri-dockerd v0.3.12 (FIXED VERSION)

CRIVERSION="0.3.12"
wget https://github.com/Mirantis/cri-dockerd/releases/download/v${CRIVERSION}/cri-dockerd-${CRIVERSION}.amd64.tgz
tar xzf cri-dockerd-${CRIVERSION}.amd64.tgz
mv cri-dockerd/cri-dockerd /usr/local/bin/
chmod +x /usr/local/bin/cri-dockerd
cri-dockerd --version  # MUST show: v0.3.12
rm -rf cri-dockerd* *.tgz


STEP 3: ALL NODES - cri-dockerd Service

wget https://raw.githubusercontent.com/Mirantis/cri-dockerd/v0.3.12/packaging/systemd/cri-docker.service -O /etc/systemd/system/cri-docker.service
wget https://raw.githubusercontent.com/Mirantis/cri-dockerd/v0.3.12/packaging/systemd/cri-docker.socket -O /etc/systemd/system/cri-docker.socket
sed -i 's|/usr/bin/cri-dockerd|/usr/local/bin/cri-dockerd|' /etc/systemd/system/cri-docker.service
systemctl daemon-reload
systemctl enable --now cri-docker.socket cri-docker.service
ls /var/run/cri-dockerd.sock  # MUST exist

STEP 4: ALL NODES - Kubernetes Tools

curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.35/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes.gpg] https://pkgs.k8s.io/core:/stable:/v1.35/deb/ /' | tee /etc/apt/sources.list.d/kubernetes.list
apt update
apt install -y kubelet=1.35.* kubeadm=1.35.* kubectl=1.35.*
apt-mark hold kubelet kubeadm kubectl

STEP 5: MASTER ONLY - Initialize

sudo kubeadm init --cri-socket unix:///var/run/cri-dockerd.sock --ignore-preflight-errors=all
or
kubeadm init --cri-socket=unix:///var/run/cri-dockerd.sock --pod-network-cidr=10.244.0.0/16

mkdir -p $HOME/.kube
cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
chown $(id -u):$(id -g) $HOME/.kube/config

STEP 6:  MASTER - Deploy Network
kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.24.1/manifests/calico.yaml


STEP 7: WORKER NODES ONLY - Join

## Run Below on Master Node to get join token 

kubeadm token create --print-join-command 

    copy the kubeadm join token from master &
           ensure to add --cri-socket unix:///var/run/cri-dockerd.sock as below &
           ensure to add sudo 
           then run on worker nodes

    Ex: sudo kubeadm join 10.128.15.231:6443  --token mks3y2.v03tyyru0gy12mbt \
           --discovery-token-ca-cert-hash sha256:3de23d42c7002be0893339fbe558ee75e14399e11f22e3f0b34351077b7c4b56 --cri-socket unix:///var/run/cri-dockerd.sock





